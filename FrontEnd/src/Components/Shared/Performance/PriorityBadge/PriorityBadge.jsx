import React from 'react';
import './PriorityBadge.css';

const PriorityBadge = () => {
    return (
        <div className="performance-shared-component PriorityBadge-component">
            <span>PriorityBadge Shared Component</span>
        </div>
    );
};

export default PriorityBadge;
