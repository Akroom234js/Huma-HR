import React from 'react';
import './DifficultyBadge.css';

const DifficultyBadge = () => {
    return (
        <div className="performance-shared-component DifficultyBadge-component">
            <span>DifficultyBadge Shared Component</span>
        </div>
    );
};

export default DifficultyBadge;
